import React from 'react';

const App = () => (
  <div className="h-screen flex items-center justify-center bg-gray-100">
    <h1 className="text-3xl font-bold underline text-blue-600">
      Hello, Electron + React + Tailwind CSS!
    </h1>
  </div>
);

export default App;
